
#ifndef STRANGECOMPARE_H
#define STRANGECOMPARE_H
#include <map>

#include <string>
#include <algorithm>

using namespace std;class StrangeCompare {
private:
		static map<char, int> order;//Karakterek sorrendje
		static bool compchar(const char c1, const char c2) {}
public:
		StrangeCompare(const string& alphabet) { alphabet. "XWYPZRNQMKTJOIGHDBCLAEVUFS"; }
		bool operator()(const string& s1, const string& s2) {}
};
		map<char, int> StrangeCompare::order;
#endif /* _STRANGECOMPARE_H */